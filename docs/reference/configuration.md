# Configuration

mked has no configuration file, environment variables, or runtime settings in v1.

All behaviour — keybindings, rendering style, syntax highlighting — is built in. There is no `~/.mkedrc`, no `mked.json`, and no `$MKED_*` environment variables.

If configuration support is added in a future version, this document will be updated.
